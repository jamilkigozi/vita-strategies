# Database Passwords - Generate secure passwords for production
WORDPRESS_DB_PASSWORD=wp_secure_$(openssl rand -hex 16)
ERPNEXT_DB_PASSWORD=erp_secure_$(openssl rand -hex 16)
MATTERMOST_DB_PASSWORD=mm_secure_$(openssl rand -hex 16)
WINDMILL_DB_PASSWORD=wm_secure_$(openssl rand -hex 16)
BOOKSTACK_DB_PASSWORD=bs_secure_$(openssl rand -hex 16)
GRAFANA_DB_PASSWORD=gf_secure_$(openssl rand -hex 16)
METABASE_DB_PASSWORD=mb_secure_$(openssl rand -hex 16)
KEYCLOAK_DB_PASSWORD=kc_secure_$(openssl rand -hex 16)

# Admin Passwords
KEYCLOAK_ADMIN_PASSWORD=admin_secure_$(openssl rand -hex 16)

# Application Settings
ENVIRONMENT=production
DOMAIN=vitastrategies.com
